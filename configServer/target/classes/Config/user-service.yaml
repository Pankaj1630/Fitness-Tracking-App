spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fitnessApp
    username: root
    password: root@12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8033

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8767/eureka/
